# 情绪地图 · 手机端 UI 改动需求文档

> **用途**：供其他 Agent 读取并执行修改。每项任务附有「验收标准」，修改完成后请逐条检查落实情况。
>
> **目标文件**：`情绪地图完整预览.html`（唯一允许修改的文件）
>
> **文件现状**：该文件目前仅存在于 `backups/root-html-backup-20260809/情绪地图完整预览.html`（334KB，8/9 15:59 快照）。当前最新版本为 `deploy/index.html`（361KB，8/9 18:14）。**执行前请先将 `deploy/index.html` 复制到项目根目录并重命名为 `情绪地图完整预览.html`，然后在此文件上进行修改。** `deploy/index.html`、`情绪记录.html` 及其他文件不得改动。
>
> **技术栈**：纯原生 HTML + CSS + JS（无框架），SVG 渲染图表，localStorage 持久化，GitHub API 云同步，DeepSeek API 提供 AI 能力。所有 CSS/JS 内联在单个 HTML 文件中。
>
> **硬性约束**：
> - 所有 UI 元素标题前不得添加图标/图片（包括 `<span class="ic"></span>` 等）
> - 底部导航保持纯文字，不添加图标
> - 回收站相关 HTML 元素必须放在主 `<script>` 标签之前，确保事件绑定正常执行
> - 事件绑定统一放入 `bindBotNav` 函数，使用 `safeInit` 保护，防止某段代码报错中断后续绑定

---

## 一、看板页面（Dashboard）

### 1.1 平均心情 / 平均睡眠 — 手机端点击弹出详情弹窗

**需求描述**：在手机端，点击看板概览区的「平均心情」和「平均睡眠」卡片时，应弹出详情弹窗，展示该指标的详细数据（如具体数值、趋势说明等）。

**当前状态**：代码中已存在 `openStatDetail(type)` 函数（约第 5651 行），通过点击概览卡片触发 `.scrim` + `.modal` 弹窗显示详情。需确认手机端触摸事件是否正常响应、弹窗内容是否完整可读。

**改动要点**：
- 确认 `#stats` 区域内心情/睡眠卡片的 `click` / `touchend` 事件在手机端正常触发
- 确认弹窗 `#statDetail` 在手机端的样式可读（字号、间距、滚动）
- 如手机端事件不响应，补充 `touchstart` / `touchend` 兼容

**验收标准**：
- [ ] 手机端点击「平均心情」卡片，弹出详情弹窗，显示平均心情数值及趋势描述
- [ ] 手机端点击「平均睡眠」卡片，弹出详情弹窗，显示平均睡眠数值及趋势描述
- [ ] 弹窗内容在小屏幕上可完整查看，无需横向滚动
- [ ] 点击弹窗外部或关闭按钮可正常关闭弹窗

---

### 1.2 情绪地形 — 手机端支持点击放大

**需求描述**：手机端「情绪地形 · 近 30 天」图表显示过小，用户难以辨识。需要支持点击放大查看，放大后可查看完整内容。情绪地形只生成近期内容即可。

**当前状态**：情绪地形位于看板 `section.panel` 内（约第 1852 行），包含「我们/自己」切换、SVG 容器 `#trend`、提示框 `#mapTip`、图例 `#trendLegend`。图表通过 Canvas 渲染，无放大交互。

**改动要点**：
- 为情绪地形区域添加点击放大交互：点击图表区域 → 全屏弹窗展示放大的情绪地形
- 放大后的图表应自适应弹窗尺寸，重新渲染以保证清晰度
- 弹窗内保留「我们/自己」切换功能
- 放大弹窗需有关闭按钮，点击外部或按钮可退出

**验收标准**：
- [ ] 手机端点击情绪地形图表区域，弹出全屏/近全屏放大视图
- [ ] 放大视图中图表清晰可辨，数据点、连线、坐标轴标签均可读
- [ ] 放大视图中「我们/自己」切换功能正常
- [ ] 放大视图中鼠标/触摸悬浮数据点有 tooltip 提示
- [ ] 点击关闭按钮或弹窗外部可正常退出放大视图

---

### 1.3 高频标签 — 标题与标签间距调整

**需求描述**：「高频标签」标题文字与下方标签卡片之间的间距过小，视觉上显得拥挤，需要调高间距。

**当前状态**：高频标签位于 `#tagPanel` 内（约第 1878 行），标题为 `<h2>` 元素，标签使用 `.tagcloud` 网格布局。`.tagcloud` 使用 `gap: 10px`（约第 406 行），手机端为 `gap: 8px`（约第 1668 行）。

**改动要点**：
- 增大 `<h2>` 标题与 `.tagcloud` 容器之间的 `margin-bottom` 或 `padding-bottom`
- 仅调整标题到首个标签行的垂直间距，不改变标签之间的间距

**验收标准**：
- [ ] 「高频标签」标题与下方第一行标签之间有明显的呼吸空间（建议 16-20px）
- [ ] 标签之间的间距保持不变（10px / 手机端 8px）
- [ ] 桌面端和手机端均已调整

---

### 1.4 【Bug · 必须修复】记一笔弹窗背景雾蒙蒙看不清输入内容

**需求描述**：点击看板页面「记一笔」按钮（或右下角 FAB）后弹出的情绪记录弹窗，背景有一层雾蒙蒙的模糊效果，导致用户看不清弹窗内的输入内容和文字。此为必须修复的 Bug。

**当前状态**：弹窗使用 `.scrim` 作为遮罩层（约第 472 行），其 CSS 为 `background: rgba(4, 7, 14, 0.6); backdrop-filter: blur(6px);`。`backdrop-filter: blur(6px)` 会对弹窗背后的内容施加模糊，但由于弹窗 `.modal` 的 `background: var(--bg-2)` 可能存在透明度或半透明问题，导致整体视觉效果「雾蒙蒙」。

**改动要点**：
- 方案 A（推荐）：降低或移除 `.scrim` 的 `backdrop-filter: blur(6px)`，改为仅使用半透明背景色（如 `background: rgba(4, 7, 14, 0.72)`），不再模糊背景
- 方案 B：保持 `.scrim` 模糊，但确保 `.modal` 的 `background` 完全不透明（使用 `--bg-2` 或固定深色值如 `#0d1117`），并增加 `box-shadow` 提升层次感
- 检查 `.modal textarea`、`.modal input` 等表单元素的背景色和文字颜色对比度，确保在弹窗内清晰可读
- 同步检查 AI 帮填弹窗 `#extractModal` 和其他使用 `.scrim` 的弹窗是否有相同问题

**验收标准**：
- [ ] 打开「记一笔」弹窗，弹窗内文字、输入框内容清晰可读，无雾蒙蒙感
- [ ] 弹窗背景仍有遮罩效果（能看出后面是暗的），但不影响弹窗内容辨识
- [ ] AI 帮填弹窗 `#extractModal` 同样清晰可读
- [ ] 深色主题和浅色主题下均正常
- [ ] 手机端和桌面端均正常

---

### 1.5 检查所有按钮可点击性

**需求描述**：全面检查看板页面（及整个应用）的所有按钮是否可以正常点击，排除事件绑定缺失、元素被遮挡、z-index 冲突等问题。

**检查范围**：
- 顶栏按钮：设置、主题、AI 帮填、记一笔
- 底部导航按钮：看板、时间轴、历史、回收站
- 悬浮 FAB 按钮（聊情绪）
- 概览区卡片点击
- 情绪地形「我们/自己」切换
- 高频标签「收起/展开」按钮
- 情绪日历日期格子点击
- 情绪热力图「我们/自己」切换
- 各弹窗内的关闭按钮、保存按钮、操作按钮

**改动要点**：
- 逐个检查上述按钮的 `click` 事件是否已绑定
- 检查是否有元素（如 `.scrim`、`.modal`、伪元素）遮挡按钮导致无法点击
- 检查 `z-index` 层级是否合理
- 检查 `pointer-events` 是否被错误设置为 `none`

**验收标准**：
- [ ] 看板页面所有按钮均可正常点击并响应
- [ ] 底部导航四个按钮均可正常切换视图
- [ ] FAB 悬浮按钮可正常打开聊情绪弹窗
- [ ] 各弹窗的关闭/保存/操作按钮均可正常点击
- [ ] 无按钮被其他元素遮挡导致无法点击的情况

---

### 1.6 悬浮按钮（FAB）支持拖动

**需求描述**：右下角悬浮按钮「聊情绪」（`#recFab`）当前固定在右下角，用户无法移动其位置。希望支持拖动，用户可将按钮拖到屏幕上任意位置，避免遮挡内容。

**当前状态**：FAB 按钮使用 `position: fixed`（约第 781 行），固定在 `right: calc(16px + env(safe-area-inset-right)); bottom: calc(24px + env(safe-area-inset-bottom))`。手机端 `bottom: calc(82px + ...)`（约第 794 行）。点击事件绑定 `on('#recFab', 'click', openChat)`（约第 5162 行）。无拖动功能。

**改动要点**：
- 使用 `pointerdown` / `pointermove` / `pointerup` 实现拖动，与标签关系图节点拖动逻辑类似
- 拖动阈值：移动距离超过 6px 才判定为拖动，否则视为点击（触发 `openChat`）
- 拖动时 FAB 跟随手指/鼠标移动，通过修改 `left` / `top`（或 `transform: translate`）定位
- FAB 拖动范围限制在视口内，不超出屏幕边界（考虑安全区域 `env(safe-area-inset-*)`）
- 拖动结束后，FAB 位置持久化到 localStorage（key 如 `mood.fabPos`），刷新后保持上次位置
- 拖动时添加视觉反馈（如缩放、阴影增强、`cursor: grabbing`）
- 拖动结束后可自动吸附到最近的屏幕边缘（可选，提升整洁度）
- 首次使用（localStorage 无保存位置）时，FAB 保持默认右下角位置

**验收标准**：
- [ ] 长按拖动 FAB，按钮跟随手指/鼠标移动
- [ ] 短按 FAB（未拖动），正常触发「聊情绪」弹窗
- [ ] FAB 不会拖出屏幕边界
- [ ] 拖动结束后刷新页面，FAB 保持在上次拖动到的位置
- [ ] 拖动时有视觉反馈（缩放/阴影变化）
- [ ] 手机端和桌面端均可正常拖动
- [ ] FAB 拖动不影响底部导航和其他按钮的点击

---

### 1.7 设置弹窗 — API Key 输入框与测试按钮同一行

**需求描述**：设置弹窗中，DeepSeek API Key 输入框和「测试」按钮应保持在同一行显示，不因弹窗宽度或屏幕尺寸换行。

**当前状态**：API Key 输入区结构如下（约第 2222-2228 行）：
```html
<div class="set-row">
  <div class="pwd-wrap">
    <input type="password" id="dsKey" placeholder="sk-..." ...>
    <button class="eye-btn" id="dsKeyEye">👁</button>
  </div>
  <button class="btn" id="dsTest">测试</button>
</div>
```
CSS 存在冲突导致换行：
- `.set-row` 设为 `display: flex; gap: 10px; flex-wrap: wrap;`（第 240 行），`flex-wrap: wrap` 允许换行
- `.set-block input[type="password"]` 设为 `width: 100%;`（第 276 行），输入框强制占满父容器宽度
- `.set-row input[type="password"]` 设为 `flex: 1; min-width: 160px;`（第 243 行），`min-width: 160px` 在窄屏下与按钮宽度之和超出容器，触发换行

**改动要点**：
- 将该 `.set-row` 的 `flex-wrap` 改为 `nowrap`（可为此行添加专用 class 如 `.set-row-nowrap`）
- 移除或覆盖 `.set-block input[type="password"]` 中 `width: 100%` 对 `#dsKey` 的影响（`#dsKey` 在 `.pwd-wrap` 内，宽度应由 `.pwd-wrap` 的 `flex: 1` 控制）
- 将 `#dsKey` 的 `min-width` 降低（如 `min-width: 80px`）或移除，确保窄屏下也能与按钮共处一行
- 确保「测试」按钮 `#dsTest` 为 `flex: 0 0 auto`，不被压缩
- 同步检查 GitHub Token 输入框（如有相同结构）是否也需要调整

**验收标准**：
- [ ] API Key 输入框与「测试」按钮在同一行显示
- [ ] 手机端（375px 宽度）下输入框和按钮仍在同一行，不换行
- [ ] 输入框仍占据剩余空间，按钮不被压缩到无法点击
- [ ] 眼睛按钮（显示/隐藏 Key）位置正常，不与测试按钮重叠
- [ ] 输入框内文字可正常输入和查看
- [ ] 桌面端弹窗（560px 宽度）下布局正常

---

### 1.8 设置弹窗 — 测试连接与立即同步按钮同一行

**需求描述**：GitHub 云同步区域的「测试连接」和「立即同步」两个按钮应保持在同一行显示，不因弹窗宽度或屏幕尺寸换行。

**当前状态**：两个按钮在同一个 `.set-row` 内（约第 2275-2278 行）：
```html
<div class="set-row">
  <button class="btn" id="ghTest">测试连接</button>
  <button class="btn primary" id="ghSync">立即同步</button>
</div>
```
`.set-row` 使用 `display: flex; gap: 10px; flex-wrap: wrap;`（第 240 行），`flex-wrap: wrap` 允许换行。手机端媒体查询中 `.set-row` 的 `gap` 改为 `8px`（第 1771 行），但 `flex-wrap` 仍为 `wrap`，窄屏下两个按钮可能换行。

**改动要点**：
- 将该 `.set-row` 的 `flex-wrap` 改为 `nowrap`（可添加专用 class）
- 两个按钮均设为 `flex: 1`，等宽分配空间
- 确保按钮文字不换行（`white-space: nowrap`）

**验收标准**：
- [ ] 「测试连接」和「立即同步」按钮在同一行显示
- [ ] 手机端（375px 宽度）下两个按钮仍在同一行
- [ ] 两个按钮宽度均衡，文字完整显示不截断
- [ ] 按钮点击功能不受影响

---

### 1.9 设置弹窗 — 数据备份区域布局优化

**需求描述**：数据备份区域有 4 个按钮（导出 JSON、导出 CSV、导入、清空缓存），当前使用 `flex-wrap: wrap` 导致在窄屏下按钮排列混乱，需要优化布局使其整齐美观。

**当前状态**：数据备份区域（约第 2289-2295 行）：
```html
<label>数据备份（可选 · 导出 / 导入 JSON 副本）</label>
<div class="set-row">
  <button class="btn" id="setExport">导出本地副本（JSON）</button>
  <button class="btn" id="setExportCsv">导出表格（CSV）</button>
  <button class="btn" id="setImportBtn">导入</button>
  <button class="btn" id="setClear">清空缓存</button>
</div>
```
4 个按钮在 `flex-wrap: wrap` 的 `.set-row` 中，手机端会随意换行，视觉混乱。

**改动要点**：
- 数据备份按钮：桌面端 2x2 网格或一行 4 列；手机端 2x2 网格，每个按钮等宽。可使用 `display: grid; grid-template-columns: repeat(2, 1fr); gap: 8px;` 替代 flex
- 「清空缓存」按钮保持 `danger` 样式，与其他按钮视觉区分
- 确保所有按钮文字在手机端完整显示

**验收标准**：
- [ ] 数据备份区域 4 个按钮排列整齐，不随意换行
- [ ] 手机端按钮为 2x2 网格布局，每个按钮等宽
- [ ] 所有按钮文字完整显示，不截断
- [ ] 按钮点击功能不受影响
- [ ] 桌面端布局同样美观整齐

---

### 1.10 移除手动空间管理，改为 AI 自动分类

**需求描述**：设置弹窗中的「空间管理」功能要求用户手动新建/切换空间（如工作/生活）来分类情绪记录，操作繁琐且容易忘记切换导致数据混淆。用户希望移除手动空间管理，改为由 AI 根据记录内容自动打标签分类——复用现有的标签体系（`tags` 数组），无需用户手动切空间。

**当前状态**：空间管理 UI 位于设置弹窗（约第 2297-2304 行）：
```html
<label style="margin-top:14px">空间管理（把数据按主题分开，如工作 / 生活）</label>
<div class="set-row">
  <select id="spaceSel" class="set-select"></select>
  <button class="btn" id="spaceNew">新建空间</button>
  <button class="btn ghost" id="spaceRename">重命名</button>
  <button class="btn danger ghost" id="spaceDel">删除</button>
</div>
<div class="set-hint" id="spaceHint"></div>
```
相关 JS 逻辑：
- `SPACE_KEY = 'mood.spaces.v1'`（约第 2445 行）：localStorage 存储多空间数据
- `loadSpaces()` / `saveSpaces()` / `applySpace()` / `refreshSpaceSel()`（约第 2944-2966 行）：空间增删改查与切换
- `persistCurrentSpace()`（约第 2468 行）：每次 `saveData()` 时把当前空间快照写回 `SPACE_KEY`
- 事件绑定（约第 5297-5326 行）：`#spaceSel` change、`#spaceNew` click、`#spaceRename` click、`#spaceDel` click
- 现有 AI 基础设施：`callDeepSeek(system, user, opts)` 函数（约第 3028 行），已有 `response_format: json_object` 支持
- 现有标签体系：每条记录的 `tags` 字符串数组，无固定标签列表约束

**改动要点**：

**A. 移除手动空间管理**
- 删除设置弹窗中「空间管理」的 `<label>` + `.set-row` + `.set-hint` 整块 HTML（约第 2297-2304 行）
- 删除 CSS 中 `/* 设置：空间管理 */` 相关样式（约第 1770 行）
- 删除 JS 中 `SPACE_KEY` 常量、`loadSpaces()` / `saveSpaces()` / `applySpace()` / `refreshSpaceSel()` / `persistCurrentSpace()` 函数
- 删除 `saveData()` 中对 `persistCurrentSpace()` 的调用（约第 2479 行），改为仅 `localStorage.setItem(LS_KEY, ...)`
- 删除事件绑定中 `#spaceSel` / `#spaceNew` / `#spaceRename` / `#spaceDel` 的监听（约第 5297-5326 行）
- **数据迁移**：如果 `localStorage` 中存在 `mood.spaces.v1`，将所有空间中的 `records` 合并到主数据 `mood.data.v2` 中（去重 by id/fingerprint），然后删除 `mood.spaces.v1`。迁移时在控制台输出合并日志

**B. 新增 AI 自动分类功能**
- 在设置弹窗原「空间管理」位置，替换为「AI 自动分类」区域：
  ```html
  <label>AI 自动分类（根据记录内容自动打标签，无需手动管理）</label>
  <div class="set-row">
    <button class="btn" id="aiClassifyBtn">立即分类</button>
    <button class="btn ghost" id="aiClassifyMerge">合并近似标签</button>
  </div>
  <div class="set-hint" id="aiClassifyHint"></div>
  ```
- 「立即分类」按钮：调用 DeepSeek API，遍历所有**缺少标签或标签为空**的记录，根据记录的 `scene`、`note`、`body`、`behavior`、`cause` 等字段内容，自动生成合适的标签
  - AI prompt 中注入用户已有的高频标签列表（从所有记录的 `tags` 中统计 Top 20），指示 AI 优先复用已有标签
  - AI 返回 JSON 格式：`{ "results": [{ "id": "记录id", "tags": ["标签1","标签2"] }, ...] }`
  - 对 AI 生成的新标签做归一化处理（去空格、统一大小写、去除标点）
  - 对 AI 生成的新标签与已有标签做模糊匹配（包含关系 / 编辑距离 ≤ 2），自动归并到最接近的已有标签
  - 每条记录最多保留 5 个标签，多余的按 AI 置信度截断
- 「合并近似标签」按钮：扫描所有记录的标签，调用 DeepSeek API 识别语义近似的标签对（如「学习」与「复习」、「疲惫」与「累」），弹出确认弹窗让用户选择是否合并，合并后统一替换所有记录中的标签
  - AI 返回 JSON：`{ "groups": [{ "canonical": "标准标签", "aliases": ["别名1","别名2"] }, ...] }`
  - 合并操作需要用户确认（`appConfirm`），不可静默执行
  - 合并后更新所有记录的 `tags` 数组并 `saveData()`

**C. 记一笔时 AI 自动建议标签**
- 在现有「AI 帮填」功能（`#extractModal` / `extractCall`）的 prompt 中增加约束：要求 AI 在提取 `tags` 时，必须优先使用用户高频标签列表中的标签
- 在 `extractCall()` 的 `sys` prompt（约第 3225 行附近）中注入用户 Top 20 高频标签：`"用户已有高频标签：学习/社交/运动/熬夜/疲惫/... 请优先使用这些标签，如不匹配再创建新标签（2-4字）"`
- AI 帮填生成的新标签同样做归一化和模糊匹配处理

**D. 看板页面增加标签分类视图（可选增强）**
- 在看板的高频标签区域或独立面板，增加「按标签分类浏览」的入口
- 点击某个标签可筛选该标签下的所有记录（现有功能已有标签筛选，确认可用即可）
- 可选：在标签云中按类别分组显示（如「活动类」学习/运动/社交、「情绪类」焦虑/放松/成就感、「状态类」疲惫/熬夜），分组由 AI 生成

**验收标准**：
- [ ] 设置弹窗中不再显示「空间管理」相关 UI（select、新建/重命名/删除按钮）
- [ ] 设置弹窗中原位置显示「AI 自动分类」区域，有「立即分类」和「合并近似标签」按钮
- [ ] 点击「立即分类」后，缺少标签的记录被 AI 自动打上标签
- [ ] AI 生成的标签优先复用用户已有标签，不会产生大量重复/近义新标签
- [ ] AI 生成的标签经过归一化处理（无多余空格、标点、大小写不一致）
- [ ] 点击「合并近似标签」后，弹出确认弹窗显示待合并的标签组
- [ ] 用户确认合并后，所有记录中的近似标签被统一替换
- [ ] 记一笔的「AI 帮填」功能在提取标签时优先使用已有高频标签
- [ ] 原有 `mood.spaces.v1` 中的数据被正确合并到主数据中，无数据丢失
- [ ] 合并后 `mood.spaces.v1` 被清除，不再产生多空间数据
- [ ] `saveData()` 不再调用 `persistCurrentSpace()`，仅写 `mood.data.v2`
- [ ] 浏览器控制台无 JS 报错
- [ ] GitHub 云同步功能不受影响（同步的是合并后的单一数据源）

---

## 二、历史页面

### 2.1 【Bug】标签关系图拖动与页面滚动冲突

**需求描述**：历史页面的「标签关系图」支持拖动节点重新摆放位置，但在手机端，当用户想向下滑动页面查看下方内容时，手指触碰标签关系图区域会触发节点拖动，导致无法正常滚动页面。拖动功能和页面滚动之间存在交互冲突。

**当前状态**：标签关系图位于 `#tagGraphCard`（约第 2054 行），SVG 容器 `#tagGraph`。节点拖动通过 `bindTagGraph()` 函数实现（约第 3775 行），使用 `pointerdown` / `pointermove` / `pointerup` 事件。背景平移通过 `bindTagGraphPan()` 函数实现（约第 3817 行），同样使用 pointer 事件。节点拖动阈值仅为 3px（约第 3797 行），极易误触。

**改动要点**：
- 提高节点拖动触发阈值（如从 3px 提高到 8-10px），减少误触
- 在节点 `pointerdown` 时不立即进入拖动模式，而是在 `pointermove` 移动距离超过阈值后才进入拖动模式；未超过阈值且 `pointerup` 时视为点击（筛选）
- 考虑为标签关系图添加「锁定/解锁」按钮：默认锁定（不可拖动，支持页面滚动），点击解锁后才能拖动节点
- 或考虑使用长按（如 500ms）进入拖动模式，短按为点击筛选

**验收标准**：
- [ ] 手机端在标签关系图区域向下滑动，页面正常滚动，不会误触拖动节点
- [ ] 手机端长按或解锁后拖动节点，节点可正常移动
- [ ] 短按节点仍可正常触发标签筛选
- [ ] 桌面端节点拖动和点击筛选功能不受影响

---

### 2.2 标签关系图 — 点击放大查看完整内容

**需求描述**：标签关系图在页面中显示空间有限，希望支持点击放大查看完整内容，放大后可以看到完整的节点网络和连接关系。

**当前状态**：标签关系图 SVG 的 `viewBox="0 0 720 420"`（约第 2067 行），支持背景平移和滚轮/双指缩放（`bindTagGraphPan`），但无点击放大为全屏视图的功能。

**改动要点**：
- 为标签关系图区域添加点击放大交互：点击图表区域（非节点）→ 全屏弹窗展示放大的标签关系图
- 放大后的 SVG 应自适应弹窗尺寸，重新计算节点布局或放大 viewBox
- 放大视图中保留平移、缩放交互
- 放大视图中点击节点仍可触发标签筛选
- 弹窗需有关闭按钮

**验收标准**：
- [ ] 点击标签关系图背景区域，弹出全屏/近全屏放大视图
- [ ] 放大视图中所有节点和连线清晰可见
- [ ] 放大视图中支持拖动平移和双指/滚轮缩放
- [ ] 放大视图中点击节点可正常触发标签筛选
- [ ] 点击关闭按钮或弹窗外部可正常退出放大视图

---

## 三、回收站页面

### 3.1 增加多选功能

**需求描述**：回收站当前只能逐条操作（恢复/彻底删除），当需要删除多条记录时非常麻烦。需要增加多选功能，支持同时选中多条记录进行批量操作。

**当前状态**：回收站列表通过 `renderTrash()` 函数渲染（约第 3470 行），每条记录有独立的「恢复」和「彻底删除」按钮（约第 3492-3493 行），无多选机制。

**改动要点**：
- 在每条回收站记录左侧添加 checkbox（复选框），用于选中/取消选中
- 添加「批量管理」按钮，点击后进入多选模式，显示 checkbox 和底部批量操作栏
- 批量操作栏包含「批量恢复」「批量彻底删除」「取消」按钮
- 多选模式下隐藏单条记录的操作按钮（或保留但灰色禁用）
- 退出多选模式后恢复单条操作模式

**验收标准**：
- [ ] 回收站页面有「批量管理」入口按钮
- [ ] 进入批量管理模式后，每条记录前出现复选框
- [ ] 可选中多条记录，选中状态有视觉反馈（如高亮/打勾）
- [ ] 底部出现批量操作栏，包含「批量恢复」「批量彻底删除」按钮
- [ ] 批量恢复/删除操作正常执行，且有确认弹窗
- [ ] 操作完成后列表正确刷新
- [ ] 可正常退出批量管理模式

---

### 3.2 增加全选功能

**需求描述**：在多选模式下，需要提供「全选」按钮，一键选中当前回收站中的所有记录，避免逐条勾选。

**改动要点**：
- 在批量管理模式下的底部操作栏或顶部添加「全选」按钮
- 点击「全选」选中所有记录，再次点击取消全选
- 全选按钮文案根据状态切换：「全选」/「取消全选」

**验收标准**：
- [ ] 批量管理模式下有「全选」按钮
- [ ] 点击「全选」可一键选中所有回收站记录
- [ ] 点击「取消全选」可一键取消所有选中
- [ ] 全选按钮文案随选中状态正确切换

---

### 3.3 删除左上角返回按钮

**需求描述**：回收站页面左上角的「← 返回」按钮是多余的，因为回收站是通过底部导航进入的独立视图，没有需要返回的前置页面。

**当前状态**：返回按钮位于 `#trashBack`（约第 6122 行），HTML 为 `<button class="page-back" id="trashBack" type="button">← 返回</button>`。点击后调用 `closeTrash()` 函数（约第 3915 行）。

**改动要点**：
- 删除 `<button class="page-back" id="trashBack">` 元素
- 删除或注释 `#trashBack` 的事件绑定代码（约第 3914-3915 行）
- 保留 `popstate` 事件监听（约第 3917 行），确保手机系统返回键仍可退出回收站
- 调整 `.page-top` 布局，确保删除返回按钮后标题区域不偏移

**验收标准**：
- [ ] 回收站页面左上角不再显示「← 返回」按钮
- [ ] 页面标题「回收站」位置正常，无偏移
- [ ] 手机系统返回键 / 浏览器后退仍可正常退出回收站
- [ ] 底部导航「回收站」按钮高亮状态正常

---

### 3.4 【Bug】彻底删除和清空按钮无法点击、无法删除内容

**需求描述**：回收站页面中的「彻底删除」按钮（单条）和「清空回收站」按钮无法点击成功，点击后没有任何反应，内容也无法被删除。

**当前状态**：
- 单条「彻底删除」按钮通过事件委托绑定在 `#trashList` 上（约第 3937 行），`data-act="purge"` 触发 `purgeFromTrash(id)` 函数
- 「清空回收站」按钮 `#trashPurgeAll` 通过 `on(pall, 'click', ...)` 绑定（约第 3927 行），触发 `purgeAllTrash()` 函数
- 根据项目记忆，此前已发生过类似问题：回收站 HTML 块放在 `<script>` 之后导致 DOM 元素在事件绑定时未被识别。需检查当前 HTML 结构是否正确放置。

**改动要点**：
- 确认回收站 HTML 块（`#trashPage` 及其子元素）在 `<script>` 标签之前（若在之后则移到之前）
- 确认 `bindBotNav` 函数中回收站相关按钮的事件绑定使用 `safeInit` 保护
- 确认 `#trashPurgeAll`、`#trashList` 等元素在 `bindBotNav` 执行时已存在于 DOM 中
- 在浏览器控制台检查点击按钮时是否有 JS 报错
- 确认 `appConfirm()` 弹窗的「确定」按钮可正常点击并返回 `true`
- 确认 `purgeFromTrash()` 和 `purgeAllTrash()` 函数逻辑正确，GitHub 同步不会导致删除失败

**验收标准**：
- [ ] 点击单条记录的「彻底删除」按钮，弹出确认弹窗
- [ ] 确认后记录从回收站列表中消失
- [ ] 点击「清空回收站」按钮，弹出确认弹窗
- [ ] 确认后所有记录从回收站列表中消失
- [ ] 删除操作后 GitHub 云同步正常（不报错）
- [ ] 刷新页面后删除结果持久化（记录不会回来）
- [ ] 浏览器控制台无 JS 报错

---

## 四、个性化适配功能（新功能）

### 4.1 AI 逐步了解用户，实现个性化适配

**需求描述**：希望 AI 能够随着用户的使用逐步了解用户的情绪模式、习惯和偏好，从而实现产品个性化适配——让界面和功能更贴合用户习惯，同时也帮助用户更了解自己的情绪规律。

**当前状态**：代码中已有基础的 AI 功能：
- DeepSeek API 直连，用于生成个性化追问（约第 3067 行，`sys` 提示词包含「基于用户近期的情绪记录，生成 3 条个性化的、开放式的反思追问」）
- AI 帮填功能（`#aiFillBtn`、`#extractModal`），通过对话式交互帮助用户从自然语言提取结构化记录
- 概览区有基础的统计功能（记录数、均值、最好最差日、高频标签、平均睡眠）
- 无用户画像 / 个性化偏好存储 / 自适应界面机制

**改动要点**：
- 设计用户画像数据结构（localStorage 存储），记录用户的情绪模式（如：常在周X情绪低落、特定标签关联低心情、睡眠与心情的关联等）
- 在 AI 生成追问时，将用户画像信息注入 prompt，使追问更有针对性
- 在概览区或单独面板展示 AI 分析出的情绪规律洞察（如「你最近3次在"学习"标签下心情都偏低，要不要试试…」）
- 可选：根据用户习惯自适应调整界面（如常用标签排序靠前、常用时间段提醒记录等）

**验收标准**：
- [ ] 系统能从用户历史记录中提取情绪模式（如标签-心情关联、时间-心情关联等）
- [ ] AI 生成的追问基于用户画像，具有个性化特征
- [ ] 界面有展示情绪规律洞察的区域
- [ ] 用户画像数据存储在 localStorage，跨会话保留
- [ ] 个性化功能不影响现有记录、同步等核心流程

---

### 4.2 记一笔功能 — AI 自动填写内容

**需求描述**：在「记一笔」功能中，希望 AI 能够自动填写内容，降低用户录入负担。用户只需输入少量信息（或口语化描述），AI 自动补全心情、精力、紧绷、睡眠、场景、标签等字段。

**当前状态**：已有 AI 帮填功能（`#aiFillBtn` → `#extractModal`），用户在对话框中输入自然语言，AI 提取结构化字段后填充到记一笔表单。但需要用户主动打开 AI 帮填弹窗，不够「自动」。

**改动要点**：
- 在「记一笔」弹窗中增加「AI 智能填写」入口，用户输入简短描述后，AI 自动分析并填充表单各字段
- 或在用户开始填写时，AI 基于当前时间、近期记录模式，预填建议值（如根据近期平均睡眠预填睡眠值）
- AI 填充的字段应有视觉标记（如浅色高亮），提示用户这些是 AI 建议值，可修改
- 保持用户可手动修改所有字段的灵活性

**验收标准**：
- [ ] 记一笔弹窗中有 AI 自动填写的入口
- [ ] 用户输入简短描述后，AI 能自动填充心情、标签、场景等字段
- [ ] AI 填充的字段有视觉标记，用户可识别哪些是 AI 填的
- [ ] 用户可手动修改 AI 填充的任何字段
- [ ] AI 填充不影响表单的保存和同步功能

---

### 4.3 排查 AI 自动填写与现有标签体系的冲突

**需求描述**：AI 自动填写功能可能会生成不在现有标签体系中的新标签，或生成的标签与用户已有标签不一致（如大小写、近义词等），需要排查潜在冲突并提供解决方案。

**当前状态**：标签存储在每条记录的 `tags` 数组中（字符串数组），无固定标签列表约束。AI 帮填时通过 prompt 指示 AI 提取标签，但无法保证 AI 生成的标签与用户历史标签一致。

**排查要点**：
- 检查 AI 帮填的 prompt 是否有约束 AI 使用已有标签的指令
- 检查 AI 生成的标签是否会创建新标签（如用户有「学习」标签，AI 可能生成「学习」或「复习」等不一致标签）
- 检查标签大小写、空格、近义词等导致标签碎片化的问题

**解决方案建议**：
- 在 AI prompt 中注入用户的高频标签列表，指示 AI 优先使用已有标签
- AI 生成标签后，与已有标签做模糊匹配（包含关系、编辑距离），自动归并到最接近的已有标签
- 对 AI 创建的新标签给予视觉标记，提示用户确认
- 可选：建立标签别名表，将常见近义词映射到统一标签

> **关联**：本节与 §1.10「移除手动空间管理，改为 AI 自动分类」紧密相关。§1.10 C 部分已要求在「AI 帮填」prompt 中注入高频标签并做模糊匹配，请结合本节一并实现，避免重复开发。

**验收标准**：
- [ ] 已排查 AI 自动填写功能与现有标签体系的潜在冲突点
- [ ] AI prompt 中已注入用户高频标签列表，指示优先使用已有标签（与 §1.10 C 一并实现）
- [ ] AI 生成的标签与已有标签做模糊匹配，自动归并近似标签
- [ ] AI 创建的新标签有视觉标记，提示用户确认
- [ ] 标签体系不会因 AI 自动填写而产生碎片化（重复/近义标签）
- [ ] 「合并近似标签」功能（§1.10 B）可定期清理已产生的碎片化标签

---

### 4.4 检查个性化适配功能当前实现状态

**需求描述**：在开发新功能前，需全面检查当前代码中是否已有个性化适配相关的实现，避免重复开发。

**检查范围**：
- AI 追问生成是否已基于用户历史记录（检查 `sys` prompt 内容）
- 是否有用户画像 / 偏好存储机制
- 是否有自适应界面（如标签排序、提醒时间等）
- AI 帮填是否已使用用户历史数据辅助填写
- 概览区统计是否已包含个性化洞察

**验收标准**：
- [ ] 已完整检查代码中与个性化适配相关的现有实现
- [ ] 已列出已有功能清单和缺失功能清单
- [ ] 新功能开发基于已有基础，不重复造轮子

---

## 五、修改完成后的全面检查

> 以下检查项在所有修改完成后逐条执行，确保每个功能点都已正确落实。

### 看板页面
- [ ] 手机端点击「平均心情」卡片 → 弹出详情弹窗，数据完整可读
- [ ] 手机端点击「平均睡眠」卡片 → 弹出详情弹窗，数据完整可读
- [ ] 手机端点击情绪地形 → 弹出放大视图，图表清晰
- [ ] 高频标签标题与标签间距已调大，视觉不拥挤
- [ ] 记一笔弹窗内容清晰可读，无雾蒙蒙感
- [ ] AI 帮填弹窗内容清晰可读
- [ ] 所有按钮均可正常点击
- [ ] 悬浮按钮可拖动到任意位置，短按仍可打开聊情绪弹窗
- [ ] 拖动后刷新页面，按钮位置保持不变
- [ ] 设置弹窗中 API Key 输入框与测试按钮在同一行，手机端不换行
- [ ] 测试连接与立即同步按钮在同一行，手机端不换行
- [ ] 数据备份区域按钮排列整齐（2x2 网格）
- [ ] 设置弹窗中「空间管理」已移除，替换为「AI 自动分类」
- [ ] 点击「立即分类」后，无标签记录被 AI 自动打上标签
- [ ] 点击「合并近似标签」可识别并合并近似标签（需用户确认）
- [ ] 原有多空间数据已合并到主数据，无数据丢失

### 历史页面
- [ ] 手机端在标签关系图区域滑动 → 页面正常滚动，不误触拖动节点
- [ ] 长按/解锁后拖动节点 → 节点可正常移动
- [ ] 点击标签关系图背景 → 弹出放大视图，内容完整
- [ ] 放大视图中平移、缩放、节点筛选均正常

### 回收站页面
- [ ] 左上角「← 返回」按钮已删除
- [ ] 「批量管理」按钮可正常进入多选模式
- [ ] 多选模式下可选中多条记录
- [ ] 「全选」按钮可一键选中所有记录
- [ ] 批量彻底删除功能正常
- [ ] 批量恢复功能正常
- [ ] 单条「彻底删除」按钮可正常点击并删除
- [ ] 「清空回收站」按钮可正常点击并清空
- [ ] 删除/清空后 GitHub 云同步正常

### 个性化适配
- [ ] 已检查现有个性化适配功能实现状态
- [ ] AI 追问基于用户历史记录，具有个性化特征
- [ ] 记一笔中 AI 自动填写功能正常
- [ ] AI 自动填写与标签体系无冲突（高频标签注入 + 模糊匹配归并）
- [ ] AI 自动分类功能正常（立即分类 + 合并近似标签）
- [ ] 标签体系不会因 AI 产生碎片化
- [ ] 个性化功能不影响现有核心流程

### 全局
- [ ] 深色主题和浅色主题下所有改动均正常
- [ ] 手机端（375px 宽度）和桌面端（1440px 宽度）均正常
- [ ] 浏览器控制台无 JS 报错
- [ ] GitHub 云同步功能不受影响
- [ ] localStorage 数据持久化正常
- [ ] 所有 UI 元素标题前无图标/图片
- [ ] 底部导航保持纯文字
