# 2026-08-12

## Mood Atlas 时间轴去竖线
- 用户反馈时间轴拥挤，要求去掉最左侧年度竖线。
- 修改 `<项目目录>\deploy\index.html` 与桌面副本 `<用户主目录>\Desktop\moodatlas\deploy\index.html`：
  - `.tl-month-body` 去掉 `border-left`，`margin-left` 从 6px 改为 0，`padding-left` 从 6px 改为 4px。
  - `.tl-month-head` 的 `margin-left` 从 10px 改为 4px。
  - `.tl-comments` 的左外边距从 26px 改为 18px。
- 版本号 `tools/VERSION`、`deploy/VERSION` 与 `__BUILD__` 从 v51 升级到 v52。
- 所有文件修改前备份到 `<临时目录>\backups\`。
- 用 puppeteer + Edge 手机视口验证：`.tl-month-body` 的 `border-left` 已为 0px，截图保存为 `<临时目录>\timeline-v52.png`。
- Node `--check` 校验三个 JS 块均通过。
- 临时文件统一使用 `<临时目录>\`。
